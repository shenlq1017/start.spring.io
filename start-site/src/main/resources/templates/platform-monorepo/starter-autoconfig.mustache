package {{starterPackage}};

import org.springframework.boot.autoconfigure.AutoConfiguration;

/**
 * {{starterName}} capability AutoConfiguration stub (skeleton only).
 */
@AutoConfiguration
public class {{autoConfigSimple}} {
}
