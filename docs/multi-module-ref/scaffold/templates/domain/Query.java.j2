package {{ base_package }}.{{ prefix }}.domain.query;

/**
 * {{ description }}领域查询条件（写侧仓储用；读侧 CQRS 直接用 contract 的 QueryRequest）
 */
public record {{ entity }}Query(String status, String keyword, long current, long size) {
}